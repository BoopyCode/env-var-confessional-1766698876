# ENV Var Confessional

A simple tool that checks if your required environment variables are properly set. Create a `.env.example` file with your required variables, then run the confessional to see what's missing. No more cryptic errors about missing API keys!

Run: `node env-confessional.js`